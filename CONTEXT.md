# archon — Domain Glossary

> The vocabulary this project reasons in. Architecture and refactor work reads this file
> to use the project's own terms (not generic words like "service" or "handler"). When a
> deepening or feature names a new domain concept, add it here in the same session.
>
> Companion: `docs/adr/` — load-bearing decisions and rejections that future surveys
> should not re-litigate.

## Project intent
(one or two sentences — what the product is, who uses it, what problem it solves)

## Core entities
- **{Entity}** — (definition; specific to this codebase, not Wikipedia-generic)

## Modules
- **{Module}** — (what it owns; what sits behind its seam)

## Roles / actors
- **{Role}** — (what they can do; what they can't; where the boundary is enforced)

## Out-of-scope concepts
- **{Concept}** — (what this project deliberately does NOT do, and why)
